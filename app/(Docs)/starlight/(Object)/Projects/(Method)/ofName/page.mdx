import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "Projects.ofName() - StarLight",
    openGraph: {
        ...openGraph,
        title: "Projects.ofName() - StarLight"
    }
}

<Section>
    # Projects.ofName()

    특정 이름의 프로젝트에 대한 [`Project`](/starlight/Project) 객체를 반환합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    Projects.ofName(name);
    ~~~

    <Section>
        ### 매개변수

        <ParamList>
            <ParamItem param={"name"} type={"String"}>
                프로젝트의 이름
            </ParamItem>
        </ParamList>
    </Section>
    <Section>
        ### 반환값

        `Project` - `name` 이름의 프로젝트에 대한 `Project` 객체를 반환합니다.
    </Section>
</Section>