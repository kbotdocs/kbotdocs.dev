import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "Project.callFunction() - StarLight",
    openGraph: {
        ...openGraph,
        title: "Project.callFunction() - StarLight"
    }
}

<Section>
    # Project.callFunction()

    프로젝트의 쓰레드 풀에서 작동 중인 `job`의 수를 반환합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    Project.callFunction(name: String, args: Any[], onException: (throwable: java.lang.Throwable) => Any);
    ~~~

    <Section>
        ### 매개변수

        <ParamList>
            <ParamItem param={"name"} type={"String"}>
                호출할 함수의 이름
            </ParamItem>
            <ParamItem param={"args"} type={"Any[]"}>
                호출할 함수에게 전달할 인자
            </ParamItem>
            <ParamItem param={"onException"} type={"(throwable) => Any"}>
                예외가 발생할 때 호출할 콜백 함수
                <Details summary={"매개변수"}>
                    <ParamList>
                        <ParamItem param={"throwable"} type={"java.lang.Throwable"}>
                            발생한 예외에 대한 `java.lang.Throwable` 값
                        </ParamItem>
                    </ParamList>
                </Details>
            </ParamItem>
        </ParamList>
    </Section>
</Section>