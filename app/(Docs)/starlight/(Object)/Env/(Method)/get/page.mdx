import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "Env.get() - StarLight",
    openGraph: {
        ...openGraph,
        title: "Env.get() - StarLight"
    }
}

<Section>
    # Env.get()

    특정 키에 대응하는 값을 반환합니다. `Env.get("KEY")`의 동작은 `Env.KEY`와 동일합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    Env.get(key);
    ~~~

    <Section>
        ### 매개변수

        <ParamList>
            <ParamItem param={"key"} type={"String"}>
                불러올 값에 대응하는 키
            </ParamItem>
        </ParamList>
    </Section>
    <Section>
        ### 반환값

        `String` - `key`에 대응하는 값을 반환합니다.
    </Section>
</Section>