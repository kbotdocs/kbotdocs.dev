import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "Env.parseString() - StarLight",
    openGraph: {
        ...openGraph,
        title: "Env.parseString() - StarLight"
    }
}

<Section>
    # Env.parseString()

    Dotenv 형식의 문자열을 `java.util.Map<String, String>` 형식으로 변환한 값을 반환합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    Env.parseString(content);
    ~~~

    <Section>
        ### 매개변수

        <ParamList>
            <ParamItem param={"content"} type={"String"}>
                `java.util.Map<String, String>` 형식으로 가져올 Dotenv 형식의 문자열
            </ParamItem>
        </ParamList>
    </Section>
    <Section>
        ### 반환값

        `java.util.Map<String, String>` - `content`를 `java.util.Map<String, String>` 형식으로 변환한 값을 반환합니다.
    </Section>
</Section>