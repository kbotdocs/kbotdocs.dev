import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "NotificationBuilder.onClick() - StarLight",
    openGraph: {
        ...openGraph,
        title: "NotificationBuilder.onClick() - StarLight"
    }
}

<Section>
    # NotificationBuilder.onClick()

    알림에 클릭 가능한 버튼과 클릭할 때 호출될 콜백 함수를 설정합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    NotificationBuilder.onClick(title, callback);
    ~~~

    <Section>
        ### 매개변수

        <ParamList>
            <ParamItem param={"title"} type={"String"}>
                버튼의 제목
            </ParamItem>
            <ParamItem param={"callback"} type={"(id) => Any?"}>
                버튼을 클릭할 때 호출될 콜백 함수
                <Details summary={"매개변수"}>
                    <ParamList>
                        <ParamItem param={"id"} type={"Number"}>
                            알림의 ID
                        </ParamItem>
                    </ParamList>
                </Details>
            </ParamItem>
        </ParamList>
    </Section>
    <Section>
        ### 반환값

        `NotificationBuilder` - 해당 `NotificationBuilder` 객체
    </Section>
</Section>