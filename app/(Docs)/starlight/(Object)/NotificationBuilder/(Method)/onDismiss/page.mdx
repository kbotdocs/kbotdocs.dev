import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "NotificationBuilder.onDismiss() - StarLight",
    openGraph: {
        ...openGraph,
        title: "NotificationBuilder.onDismiss() - StarLight"
    }
}

<Section>
    # NotificationBuilder.onDismiss()

    알림이 상단바에서 제거될 때 호출될 콜백 함수를 설정합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    NotificationBuilder.onDismiss(callback);
    ~~~

    <Section>
        ### 매개변수

        <ParamList>
            <ParamItem param={"callback"} type={"(id) => Any?"}>
                알림이 상단바에서 제거될 때 호출될 콜백 함수
                <Details summary={"매개변수"}>
                    <ParamList>
                        <ParamItem param={"id"} type={"Number"}>
                            알림의 ID
                        </ParamItem>
                    </ParamList>
                </Details>
            </ParamItem>
        </ParamList>
    </Section>
    <Section>
        ### 반환값

        `NotificationBuilder` - 해당 `NotificationBuilder` 객체
    </Section>
</Section>