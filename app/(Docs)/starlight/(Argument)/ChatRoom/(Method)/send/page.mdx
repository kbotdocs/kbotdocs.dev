import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "ChatRoom.send() - StarLight",
    openGraph: {
        ...openGraph,
        title: "ChatRoom.send() - StarLight"
    }
}

<Section>
    # ChatRoom.send()

    방에 메시지를 발신합니다. 발신 성공 여부를 반환합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    room.send(message);
    ~~~

    <Section>
        ### 매개변수

        <ParamList>
            <ParamItem param={"message"} type={"String"}>
                발신할 메시지의 내용
            </ParamItem>
        </ParamList>
    </Section>
    <Section>
        ### 반환값

        `Boolean` - 발신에 성공할 경우 `true`, 실패할 경우 `false`를 반환합니다.
    </Section>
</Section>