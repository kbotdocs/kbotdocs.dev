import {compatibility} from "@/constants/compatibility/legacy/eventListener/response";
import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "response 이벤트 리스너 - 레거시 API",
    openGraph: {
        ...openGraph,
        title: "response 이벤트 리스너 - 레거시 API"
    }
}

<Section>
    # response

    `response`는 메시지를 수신할 때 동작하는 이벤트 리스너입니다.
</Section>
<Section>
    ## 구문

    ~~~js
    function response(room, msg, sender, isGroupChat, replier, imageDB, packageName, isMention, logId, channelId, userHash) {

    }
    ~~~

    <Section>
        ### 인자

        <ParamList>
            <ParamItem param={"room"} type={"String"}>
                메시지가 발신된 방의 이름입니다.
            </ParamItem>
            <ParamItem param={"msg"} type={"String"}>
                메시지의 전체 내용입니다.
            </ParamItem>
            <ParamItem param={"sender"} type={"String"}>
                발신자의 이름입니다.
            </ParamItem>
            <ParamItem param={"isGroupChat"} type={"Boolean"}>
                방이 그룹 채팅방인지의 여부입니다.
            </ParamItem>
            <ParamItem param={"replier"} type={"Replier"}>
                메시지 발신을 위한 [`Replier`](/legacy/Replier) 객체입니다.
            </ParamItem>
            <ParamItem param={"imageDB"} type={"ImageDB"}>
                발신된 이미지와 발신자의 프로필 이미지에 대한 [`ImageDB`](/legacy/ImageDB) 객체입니다.
            </ParamItem>
            <ParamItem param={"packageName"} type={"String"}>
                메시지가 발신된 메신저 앱의 패키지명입니다.
            </ParamItem>
            <ParamItem param={"isMention"} type={"Boolean"}>
                메시지 내 멘션 포함 여부입니다.
            </ParamItem>
            <ParamItem param={"logId"} type={"BigInt"}>
                메시지에 대한 고유 ID입니다.
            </ParamItem>
            <ParamItem param={"channelId"} type={"BigInt"}>
                메시지가 발신된 방의 고유 ID입니다.
            </ParamItem>
            <ParamItem param={"userHash"} type={"String"}>
                발신자의 고유 해시입니다. 해시는 같은 방 내에서 고유합니다.
            </ParamItem>
        </ParamList>
    </Section>
</Section>
<Section>
    ## 앱 호환성

    <AppCompat compats={Object.values(compatibility)}/>
</Section>