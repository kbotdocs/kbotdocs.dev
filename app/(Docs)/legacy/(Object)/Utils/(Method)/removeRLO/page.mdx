import {compatibility} from "@/constants/compatibility/legacy/object/Utils";
import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "Utils.removeRLO() - 레거시 API",
    openGraph: {
        ...openGraph,
        title: "Utils.removeRLO() - 레거시 API"
    }
}

<Section>
    # Utils.removeRLO()

    문자열 `str`에서 반전 문자를 삭제한 값을 반환합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    Utils.removeRLO(str);
    ~~~

    <Section>
        ### 매개변수

        <ParamList>
            <ParamItem param={"str"} type={"String"}>
                반전 문자를 삭제할 문자열
            </ParamItem>
        </ParamList>
    </Section>
    <Section>
        ### 반환값

        `String` - 문자열 `str`에서 반전 문자를 삭제한 값을 반환합니다.
    </Section>
</Section>
<Section>
    ## 앱 호환성

    <AppCompat compats={compatibility["removeRLO"]}/>
</Section>