import {compatibility} from "@/constants/compatibility/legacy/object/Utils";
import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "Utils.sleep() - 레거시 API",
    openGraph: {
        ...openGraph,
        title: "Utils.sleep() - 레거시 API"
    }
}

<Section>
    # Utils.sleep()

    일정 시간(밀리초; ms)만큼 지연합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    Utils.sleep(mil);
    ~~~

    <Section>
        ### 매개변수

        <ParamList>
            <ParamItem param={"mil"} type={"Number"}>
                지연할 시간(밀리초; ms)
            </ParamItem>
        </ParamList>
    </Section>
</Section>
<Section>
    ## 앱 호환성

    <AppCompat compats={compatibility["sleep"]}/>
</Section>