import {compatibility} from "@/constants/compatibility/legacy/object/Utils";
import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "Utils.toast() - 레거시 API",
    openGraph: {
        ...openGraph,
        title: "Utils.toast() - 레거시 API"
    }
}

<Section>
    # Utils.toast()

    토스트 메시지를 표시합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    Utils.toast(msg);
    ~~~

    <Section>
        ### 매개변수

        <ParamList>
            <ParamItem param={"msg"} type={"String"}>
                표시할 토스트 메시지의 내용
            </ParamItem>
        </ParamList>
    </Section>
</Section>
<Section>
    ## 앱 호환성

    <AppCompat compats={compatibility["toast"]}/>
</Section>