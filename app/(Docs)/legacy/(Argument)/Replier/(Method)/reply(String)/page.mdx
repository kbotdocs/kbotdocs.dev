import {compatibility} from "@/constants/compatibility/legacy/argument/Replier";
import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "Replier.reply(String) - 레거시 API",
    openGraph: {
        ...openGraph,
        title: "Replier.reply(String) - 레거시 API"
    }
}

<Section>
    # Replier.reply(String)

    메시지가 발신된 방에 메시지를 발신합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    replier.reply(msg);
    ~~~

    <Section>
        ### 매개변수

        <ParamList>
            <ParamItem param={"msg"} type={"String"}>
                발신할 메시지의 내용
            </ParamItem>
        </ParamList>
    </Section>
</Section>
<Section>
    ## 앱 호환성

    <AppCompat compats={compatibility["reply(String)"]}/>
</Section>