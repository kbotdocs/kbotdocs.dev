import {compatibility} from "@/constants/compatibility/api2/object/Security";
import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "Security.ariaDecode() - API2",
    openGraph: {
        ...openGraph,
        title: "Security.ariaDecode() - API2"
    }
}

<Section>
    # Security.ariaDecode()

    `value`를 ARIA 복호화한 값을 반환합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    Security.ariaDecode(key, value);
    ~~~

    <Section>
        ### 매개변수

        <ParamList>
            <ParamItem param={"key"} type={"String"}>
                ARIA 복호화를 위한 키
            </ParamItem>
            <ParamItem param={"value"} type={"String"}>
                복호화할 값
            </ParamItem>
        </ParamList>
    </Section>
    <Section>
        ### 반환값

        `String` - `value`를 ARIA 복호화한 값을 반환합니다.
    </Section>
</Section>
<Section>
    ## 앱 호환성

    <AppCompat compats={compatibility["ariaDecode"]}/>
</Section>