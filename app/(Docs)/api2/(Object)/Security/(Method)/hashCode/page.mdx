import {compatibility} from "@/constants/compatibility/api2/object/Security";
import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "Security.hashCode() - API2",
    openGraph: {
        ...openGraph,
        title: "Security.hashCode() - API2"
    }
}

<Section>
    # Security.hashCode()

    `value`의 해시코드를 반환합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    Security.hashCode(value);
    ~~~

    <Section>
        ### 매개변수

        <ParamList>
            <ParamItem param={"value"} type={"String"}>
                해시코드를 추출할 값
            </ParamItem>
        </ParamList>
    </Section>
    <Section>
        ### 반환값

        `String` - `value`의 해시코드를 반환합니다.
    </Section>
</Section>
<Section>
    ## 앱 호환성

    <AppCompat compats={compatibility["hashCode"]}/>
</Section>