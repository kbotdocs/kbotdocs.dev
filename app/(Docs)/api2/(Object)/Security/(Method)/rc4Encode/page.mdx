import {compatibility} from "@/constants/compatibility/api2/object/Security";
import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "Security.rc4Encode() - API2",
    openGraph: {
        ...openGraph,
        title: "Security.rc4Encode() - API2"
    }
}

<Section>
    # Security.rc4Encode()

    `value`를 RC4 암호화한 값을 반환합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    Security.rc4Encode(key, value);
    ~~~

    <Section>
        ### 매개변수

        <ParamList>
            <ParamItem param={"key"} type={"String"}>
                RC4 암호화를 위한 키
            </ParamItem>
            <ParamItem param={"value"} type={"String"}>
                암호화할 값
            </ParamItem>
        </ParamList>
    </Section>
    <Section>
        ### 반환값

        `String` - `value`를 RC4 암호화한 값을 반환합니다.
    </Section>
</Section>
<Section>
    ## 앱 호환성

    <AppCompat compats={compatibility["rc4Encode"]}/>
</Section>