import {compatibility} from "@/constants/compatibility/api2/object/Security";
import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "Security.aesEncode() - API2",
    openGraph: {
        ...openGraph,
        title: "Security.aesEncode() - API2"
    }
}

<Section>
    # Security.aesEncode()

    `value`를 AES 암호화한 값을 반환합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    Security.aesEncode(key, initVector, value);
    ~~~

    <Section>
        ### 매개변수

        <ParamList>
            <ParamItem param={"key"} type={"String"}>
                AES 암호화를 위한 키
            </ParamItem>
            <ParamItem param={"initVector"} type={"String"}>
                초기화 백터
            </ParamItem>
            <ParamItem param={"value"} type={"String"}>
                암호화할 값
            </ParamItem>
        </ParamList>
    </Section>
    <Section>
        ### 반환값

        `String` - `value`를 AES 암호화한 값을 반환합니다.
    </Section>
</Section>
<Section>
    ## 앱 호환성

    <AppCompat compats={compatibility["aesEncode"]}/>
</Section>