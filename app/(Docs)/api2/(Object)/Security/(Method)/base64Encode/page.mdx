import {compatibility} from "@/constants/compatibility/api2/object/Security";
import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "Security.base64Encode() - API2",
    openGraph: {
        ...openGraph,
        title: "Security.base64Encode() - API2"
    }
}

<Section>
    # Security.base64Encode()

    `value`를 Base64 암호화한 값을 반환합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    Security.base64Encode(value);
    ~~~

    <Section>
        ### 매개변수

        <ParamList>
            <ParamItem param={"value"} type={"String"}>
                암호화할 값
            </ParamItem>
        </ParamList>
    </Section>
    <Section>
        ### 반환값

        `String` - `value`를 Base64 암호화한 값을 반환합니다.
    </Section>
</Section>
<Section>
    ## 앱 호환성

    <AppCompat compats={compatibility["base64Encode"]}/>
</Section>