import {compatibility} from "@/constants/compatibility/api2/object/Broadcast";
import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "Broadcast.unregister() - API2",
    openGraph: {
        ...openGraph,
        title: "Broadcast.unregister() - API2"
    }
}

<Section>
    # Broadcast.unregister()

    `broadcastName`를 이름으로 하는 브로드캐스트에 대한 특정 리스너를 제거합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    Broadcast.unregister(broadcastName, task);
    ~~~

    <Section>
        ### 매개변수

        <ParamList>
            <ParamItem param={"broadcastName"} type={"String"}>
                리스너를 제거할 브로드캐스트 이름
            </ParamItem>
            <ParamItem param={"task"} type={"(value) => Any"}>
                제거할 리스너
                <Details summary={"매개변수"}>
                    <ParamList>
                        <ParamItem param={"value"} type={"Any"}>
                            브로드캐스트을 통해 송출된 값
                        </ParamItem>
                    </ParamList>
                </Details>
            </ParamItem>
        </ParamList>
    </Section>
</Section>
<Section>
    ## 앱 호환성

    <AppCompat compats={compatibility["unregister"]}/>
</Section>