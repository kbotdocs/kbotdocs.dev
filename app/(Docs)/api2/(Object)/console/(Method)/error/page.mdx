import {compatibility} from "@/constants/compatibility/api2/object/console";
import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "console.error() - API2",
    openGraph: {
        ...openGraph,
        title: "console.error() - API2"
    }
}

<FeatureStatusNoti featureStatus={compatibility["error"].status} />
<Section>
    ## console.error()

    에러 로그를 작성합니다. `showToast`의 값이 `true`일 경우 작성된 로그를 토스트 메시지로 출력합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    console.error(str);
    console.error(str, showToast);
    ~~~

    <Section>
        ### 매개변수

        <ParamList>
            <ParamItem param={"str"} type={"String"}>
                작성할 에러 로그의 내용
            </ParamItem>
            <ParamItem param={"showToast"} type={"Boolean"} defaultVal={"`false`"} optional>
                작성된 로그를 토스트 메시지로 출력할 것인지의 여부
            </ParamItem>
        </ParamList>
    </Section>
</Section>
<Section>
    ## 앱 호환성

    <AppCompat compats={compatibility["error"]}/>
</Section>