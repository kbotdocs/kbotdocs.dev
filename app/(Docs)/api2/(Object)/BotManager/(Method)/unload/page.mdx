import {compatibility} from "@/constants/compatibility/api2/object/BotManager";
import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "BotManager.unload() - API2",
    openGraph: {
        ...openGraph,
        title: "BotManager.unload() - API2"
    }
}

<Section>
    # BotManager.unload()

    `botName` 이름의 `Bot` 객체를 컴파일 전 상태로 전환(언로드)합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    BotManager.unload(botName);
    ~~~

    <Section>
        ### 매개변수

        <ParamList>
            <ParamItem param={"botName"} type={"String"}>
                언로드할 `Bot` 객체의 이름
            </ParamItem>
        </ParamList>
    </Section>
</Section>
<Section>
    ## 앱 호환성

    <AppCompat compats={compatibility["unload"]}/>
</Section>