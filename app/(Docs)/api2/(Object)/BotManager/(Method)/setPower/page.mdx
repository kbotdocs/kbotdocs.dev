import {compatibility} from "@/constants/compatibility/api2/object/BotManager";
import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "BotManager.setPower() - API2",
    openGraph: {
        ...openGraph,
        title: "BotManager.setPower() - API2"
    }
}

<Section>
    # BotManager.setPower()

    `botName` 이름의 `Bot` 객체의 활성화 여부를 제어합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    BotManager.setPower(botName, power);
    ~~~

    <Section>
        ### 매개변수

        <ParamList>
            <ParamItem param={"botName"} type={"String"}>
                활성화 여부를 제어할 `Bot` 객체의 이름
            </ParamItem>
            <ParamItem param={"power"} type={"Boolean"}>
                `Bot` 객체의 활성화 여부
            </ParamItem>
        </ParamList>
    </Section>
</Section>
<Section>
    ## 앱 호환성

    <AppCompat compats={compatibility["setPower"]}/>
</Section>