import {compatibility} from "@/constants/compatibility/api2/object/AppData";
import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "AppData.getInt() - API2",
    openGraph: {
        ...openGraph,
        title: "AppData.getInt() - API2"
    }
}

<Section>
    # AppData.getInt()

    `key`에 대응하는 `Number`(정수)형 데이터를 반환합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    AppData.getInt(key);
    ~~~

    <Section>
        ### 매개변수

        <ParamList>
            <ParamItem param={"key"} type={"String"}>
                불러올 `Number`(정수)형 데이터에 대응하는 키
            </ParamItem>
        </ParamList>
    </Section>
    <Section>
        ### 반환값

        `Number` - `key`에 대응하는 `Number`(정수)형 데이터를 반환합니다.
    </Section>
</Section>
<Section>
    ## 앱 호환성

    <AppCompat compats={compatibility["getInt"]}/>
</Section>