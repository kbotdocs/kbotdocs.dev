import {compatibility} from "@/constants/compatibility/api2/object/AppData";
import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "AppData.getString() - API2",
    openGraph: {
        ...openGraph,
        title: "AppData.getString() - API2"
    }
}

<Section>
    # AppData.getString()

    `key`에 대응하는 `String`형 데이터를 반환합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    AppData.getString(key);
    ~~~

    <Section>
        ### 매개변수

        <ParamList>
            <ParamItem param={"key"} type={"String"}>
                불러올 `String`형 데이터에 대응하는 키
            </ParamItem>
        </ParamList>
    </Section>
    <Section>
        ### 반환값

        `String` - `key`에 대응하는 `String`형 데이터를 반환합니다.
    </Section>
</Section>
<Section>
    ## 앱 호환성

    <AppCompat compats={compatibility["getString"]}/>
</Section>