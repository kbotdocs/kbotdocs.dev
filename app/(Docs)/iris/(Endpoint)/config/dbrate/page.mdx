import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "/config/dbrate - Iris",
    openGraph: {
        ...openGraph,
        title: "/config/dbrate - Iris"
    }
}

<Section>
    # /config/dbrate

    데이터베이스 폴링 지연 시간을 업데이트합니다. 이 값을 조정하면 Iris가 데이터베이스에서 새로운 메시지를 얼마나 자주 확인하는지 변경됩니다.

    <Noti type="warning">
        낮은 `rate` 값은 메시지를 더 즉각적으로 감지할 수 있지만 CPU 사용량을 증가시킵니다.
    </Noti>

    <Table>
        <colgroup>
            <col width={"200px"}/>
            <col />
        </colgroup>
        <Tbody>
            <Tr>
                <Td><span className={"text-sm"}>**엔드포인트**</span></Td>
                <Td>
                    <Endpoint method={"GET"}>`/config/dbrate`</Endpoint>
                </Td>
            </Tr>
            <Tr>
                <Td><span className={"text-sm"}>**Content-Type**</span></Td>
                <Td>
                    `-/-`
                </Td>
            </Tr>
        </Tbody>
    </Table>
</Section>
<Section>
    ## 요청

    <Section>
        ### 쿼리

        ~~~http
        /config/dbrate?rate=[DATABASE_POLLING_INTERVAL_IN_MILLISECONDS]
        ~~~

        <ParamList caption={"쿼리 매개변수"}>
            <ParamItem param={"rate"} type={"int"}>
                업데이트할 데이터베이스 폴링 지연 시간(밀리초; ms)입니다.
            </ParamItem>
        </ParamList>
    </Section>
    <Section>
        ### 예제

        ~~~bash
        curl http://[YOUR_DEVICE_IP]:[bot_http_port]/config/dbrate?rate=300
        ~~~
    </Section>
</Section>